services:
  mongo:
    image: mongo:7
    container_name: das-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - das_mongo_data:/data/db

volumes:
  das_mongo_data:
